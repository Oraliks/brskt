/**
 * Client auth helpers — on n'a aucun signIn/signOut exposé côté client
 * autre que TelegramLoginButton (qui hit /api/auth/telegram) et
 * logoutAction (Server Action). Ce fichier est volontairement vide.
 */

export {};
